h1 Raum #{@office.name}

.row
  .col
    p Here works #{@office.employees.count} Employees

    - if @office.employees.present?
      ul
        - @office.employees.each do |employee|
          li = link_to employee.name, employee_path(employee)
  .col
     = render '/comments/comments', thing: @office